This directory contains files, tasks and projects related to
Static libraries in C-Programming Language

The files included in this directory and its functions includes

	-- create_static_lib.sh - which is a script that creates a static library and called liball.a from all the .c files that are in the present directory
	-- main.h - which is a header file that contains all prototypes of the function stored in a library
